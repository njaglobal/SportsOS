# Application Services

Application services coordinate multiple use-cases or encapsulate orchestration
that does not belong to a single use-case. Repository contracts, when needed,
are declared here or beside their use-case — never as a single generic CRUD
interface. Added with real vertical slices; none exist yet, by design.
