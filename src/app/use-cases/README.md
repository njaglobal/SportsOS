# Use-Cases

One orchestration per operation, each implementing `UseCase<Input, Output>`
from `@app/contracts`. Use-cases are added alongside real vertical slices in
later sprints — none exist yet, by design (Sprint 1 builds infrastructure, not
product behavior).
